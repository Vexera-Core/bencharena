export function Footer() {
  return (
    <footer className="footerPanel" id="docs" aria-label="BenchArena footer">
      <span>BenchArena foundation shell</span>
      <p>No backend, wallet, live sandbox, MCP server, Solana receipt, database, or x402 flow is active in this frontend.</p>
      <a href="#main-content">Back to top</a>
    </footer>
  );
}
