name: Tests Passing

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests:
    name: Tests Passing
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Placeholder test suite
        run: |
          echo "BenchArena placeholder CI passed."
          echo "Real lint, typecheck, test, and build checks will be added later."
          exit 0